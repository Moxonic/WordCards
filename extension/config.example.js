// Copy this file to `config.js` and fill in the two values from the SAME Firebase
// project the web app uses (Firebase console -> Project settings -> General).
// These are not secrets - they are the same values shipped in the web bundle -
// but `config.js` is gitignored so you don't have to think about it.

export const FIREBASE_API_KEY = 'YOUR_WEB_API_KEY';
export const FIREBASE_PROJECT_ID = 'your-project-id';
